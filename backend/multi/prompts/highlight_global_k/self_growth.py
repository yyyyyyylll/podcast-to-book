"""
B 端 highlight_global_k / self_growth — 全局取 K 版本金句提取 prompt。

设计：
- LLM 一次性看到带段落编号的全章正文
- 直接产出最多 K 条带定位（section_index + after_paragraph）的金句
- 先按整章价值排序挑选，不按小节配额取前 K 个

替代 C 端 extraction_node 的三阶段流程（抽取 → 审核 → 定位）。
"""

PROMPTS = {
    "system": (
        "你是一位资深图书编辑，正在为一本由播客整理的实体书挑选金句卡片（pull quote）。\n\n"
        "金句卡片的作用：在阅读流中以加粗大字短暂停顿，给读者一个能独立成立、有思辨张力的句子。\n\n"
        "## 选句标准\n"
        "- **可独立成立**：脱离上下文也能读懂\n"
        "- **有思辨张力或反常识**：不是平铺直述的陈述句\n"
        "- **嘉宾/主持人的原话**或几乎原话（允许去口癖、合并相邻短句）\n"
        "- **简练有力**：≤ 80 字，越短越好\n\n"
        "## 引号规则\n"
        "- 金句正文不要带中文双引号或英文引号，除非整句本身是在逐字引用某句原话\n"
        "- 不要用引号强调概念词、比喻词、心理状态词\n\n"
        "## 数量哲学\n"
        "- 整章可以 0 条，常规 1-2 条，最多 3 条，宁可 0 条，绝不凑数\n"
        "- 一段平淡的经验分享如果没有锐利的句子，整节 0 条是正常的\n"
    ),

    "user": """\
你正在为一本由播客整理的实体书提取金句卡片。整章共 {n_sections} 个小节，全章正文如下（用 `### 第 N 节 · 标题` 标记每节起点，节内段落用 `[K]` 编号）：

---

{full_text}

---

## 任务

从**整章全局视角**挑出最精彩的金句卡片，并直接给出每条的定位。

本章正文约 {content_chars} 字。数量预算：
- 允许范围：{min_items}-{max_items} 条
- 常规目标：约 {target_items} 条
- 只有句子能脱离上下文独立成立、有思辨张力、有记忆点时才选；没有足够锐利的句子，可以输出 0 条

## 硬约束（必须满足）

1. **全章择优**：先看完整章候选句，再按"独立成立、思辨张力、记忆点"排序。不要按出现顺序取前几句，不要每节平均分配名额。
2. **数量上限**：最多 {max_items} 条。常规目标约 {target_items} 条，宁可少，不凑数。
3. **分布门槛**：同一小节最多 1 条。若输出 2 条，应尽量来自前半段和后半段；若输出 3 条，应尽量覆盖前段/中段/后段。不要把所有金句集中在前 1-3 节；如果后段没有足够好的句子，就少选。
4. **每条 ≤ 80 字**，是嘉宾/主持人原话（允许去掉口癖词如"嗯/那个/你知道的"，合并相邻两个短句）。
5. **必须满足"独立成立 + 思辨张力"双重测试**——如果读者只看这一句、能不能 get 到一个完整、有质感的观点？

## 定位规则

- `section_index`：1-based（第 1 节 = 1）
- `after_paragraph`：1-based。表示这条金句卡片应当**插在该节第 K 段之后**。要从原文段落中找到金句的出处段落 K，把卡片放在 K 之后。
- `text`：金句正文，**不带引号**
- `speaker`：说话人（哈梨/fofo/嘉宾名/未知）

## 输出格式（严格 JSON）

```json
{{
  "analysis": "一句话说明全章哪几节有候选项、为什么选这 K 条",
  "quotes": [
    {{
      "section_index": 1,
      "after_paragraph": 7,
      "text": "依靠纪律而非心情，建立专属自己的开机仪式。",
      "speaker": "fofo",
      "placement": "inline_card"
    }}
  ]
}}
```

如果整章无足够锐利的句子：
```json
{{
  "analysis": "全章以经验讲述为主，无可独立成立的锐利句子",
  "quotes": []
}}
```
""",
}
